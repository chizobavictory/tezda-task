//WIP @sampson 
// 1. Create endpoint that resends the verification link.
//  2. it gets the verification status by taking in the user_id, and searchin the DB, to see if verified is false.
// if false, it resends the email address passed in, the verification link.
// returnns, true or false, using build response

export async function resendEmailVerification(){

}